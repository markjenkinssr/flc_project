{% load static %}
<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8"><title>Session Finished</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container mt-5">
  <div class="card">
    <div class="card-body">
      <h2 class="mb-3">You’re all set, {{ advisor.first_name }}!</h2>

      {% if registrations %}
        <ul class="mb-3">
          {% for r in registrations %}
            <li>{{ r.first_name }} {{ r.last_name }}</li>
          {% endfor %}
        </ul>
      {% else %}
        <p class="text-muted">No participants yet.</p>
      {% endif %}

      <div class="d-flex gap-2">
        <a class="btn btn-primary" href="{% url 'registrations:registration_form' advisor.id %}">Add more</a>
        <a class="btn btn-outline-secondary" href="{% url 'registrations:user_access' %}">Back to Access</a>
      </div>
    </div>
  </div>
</body>
</html>

